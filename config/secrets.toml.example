# Myriad Secrets Configuration
# Copy this file to secrets.toml and fill in your credentials
# DO NOT commit secrets.toml to version control!

# OPNsense API credentials
# Generate API key/secret in OPNsense: System > Access > Users > [user] > API keys
[opnsense.main]
api_key = "your-api-key-here"
api_secret = "your-api-secret-here"

# Unifi Controller credentials (Phase 3)
# [unifi.main]
# username = "admin"
# password = "your-password-here"

# SSH key paths (Phase 2)
# [ssh.homelab]
# key_path = "/home/user/.ssh/homelab_ed25519"
# passphrase = ""  # Leave empty if key has no passphrase
